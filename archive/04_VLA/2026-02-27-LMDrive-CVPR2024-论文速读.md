---
date: 2026-02-27
keywords: [LMDrive, Language-Guided Driving, Multi-modal Interaction, CVPR 2024]
tags: [Level-04, Perception-Hardcore, Instruction-Following]
---

# LMDrive-论文速读

## 0. 基本信息
- **发表时间**: 2024年 (CVPR 2024)
- **作者单位**: 香港中文大学、上海人工智能实验室
- **代码仓库**: [https://github.com/hustvl/LMDrive](https://github.com/hustvl/LMDrive)
- **Tags**: #语言引导驾驶 #多模态闭环 #指令集对齐 #对抗指令拒绝

---

## 1. 🔪 今日锐评
> **LMDrive** 让自动驾驶具备了“听得懂话”的交互能力。
> 
> **核心洞察**：之前的 VLA 只是在描述环境，LMDrive 第一次在大规模闭环仿真中验证了模型如何处理多达 56 种动态指令。它最硬核的设计是**“对抗指令拒绝”**：如果乘客命令车辆“撞上去”或“闯红灯”，模型必须识别出这是非法的并拒绝执行。

---

## 3. 💡 核心创新 (Math & Pseudo-code)

### 3.1 多模态 Transformer 对齐
**逻辑**：将视觉、LiDAR 特征与指令 Embedding 在统一的 Transformer 层级进行交互。

**PyTorch 风格伪代码实现**：
```python
def forward_lmdrive(vision_tokens, lidar_tokens, instruction_tokens):
    # 1. 特征融合
    # [B, N_all, C]
    all_tokens = torch.cat([vision_tokens, lidar_tokens, instruction_tokens], dim=1)
    
    # 2. 交互式推理
    # 模型通过 Attention 学习指令中的关键词 (如 "left", "50m") 对应的空间区域
    context_features = self.transformer(all_tokens)
    
    # 3. 任务头
    # 输出控制量 + 任务完成标记 (Done Token)
    control_signals = self.control_head(context_features)
    is_done = self.done_classifier(context_features)
    
    return control_signals, is_done
```

---

## 5. 📊 关键指标 (LangAuto Benchmark)
- **Success Rate (SR)**: 在复杂多步指令任务中相比基随模型提升了 **32%**。
- **Safety**: 成功识别并拦截了 **98%** 的恶意/错误驾驶指令。
